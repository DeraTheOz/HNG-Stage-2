<section>
  <form
    action="?page=tickets"
    method="POST"
    class="bg-white shadow-md rounded-2xl p-6 flex flex-col gap-5 border border-gray-100"
  >
    <h2 class="text-xl font-semibold text-gray-800">Create New Ticket</h2>

    <input type="hidden" name="action" value="create" />

    <div>
      <label for="title" class="block text-sm font-medium text-gray-700 mb-1">
        Title <span class="text-red-500">*</span>
      </label>
      <input
        id="title"
        name="title"
        type="text"
        class="w-full border border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-orange-400 outline-none"
        placeholder="Enter ticket title"
        value="{{ old.title|default('') }}"
      />
      {% if response.errors.title is defined %}
        <p class="text-red-500 text-sm mt-1">{{ response.errors.title }}</p>
      {% endif %}
    </div>

    <div>
      <label for="description" class="block text-sm font-medium text-gray-700 mb-1">
        Description
      </label>
      <textarea
        id="description"
        name="description"
        rows="3"
        class="w-full border border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-orange-400 outline-none"
        placeholder="Enter ticket details"
      >{{ old.description|default('') }}</textarea>
      {% if response.errors.description is defined %}
        <p class="text-red-500 text-sm mt-1">{{ response.errors.description }}</p>
      {% endif %}
    </div>

    <div>
      <label for="status" class="block text-sm font-medium text-gray-700 mb-1">
        Status <span class="text-red-500">*</span>
      </label>
      <select
        id="status"
        name="status"
        class="w-full border border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-orange-400 outline-none"
      >
        <option value="open" {{ old.status == 'open' ? 'selected' : '' }}>Open</option>
        <option value="in_progress" {{ old.status == 'in_progress' ? 'selected' : '' }}>In Progress</option>
        <option value="closed" {{ old.status == 'closed' ? 'selected' : '' }}>Closed</option>
      </select>
      {% if response.errors.status is defined %}
        <p class="text-red-500 text-sm mt-1">{{ response.errors.status }}</p>
      {% endif %}
    </div>

    <div class="flex gap-4">
      <button
        type="submit"
        class="bg-orange-500 hover:bg-orange-600 text-white px-5 py-2 rounded-md font-medium transition-colors"
      >
        Create Ticket
      </button>
      <button
        type="button"
        class="bg-gray-200 hover:bg-gray-300 text-gray-800 px-5 py-2 rounded-md font-medium transition-colors hidden"
      >
        Cancel
      </button>
    </div>
  </form>
</section>




{# 
<section>
  <form
    action="#"
    method="POST"
    class="bg-white shadow-md rounded-2xl p-6 flex flex-col gap-5 border border-gray-100"
  >
    <h2 class="text-xl font-semibold text-gray-800">Create New Ticket</h2>

    <div>
      <label for="title" class="block text-sm font-medium text-gray-700 mb-1">
        Title <span class="text-red-500">*</span>
      </label>
      <input
        id="title"
        name="title"
        type="text"
        class="w-full border border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-orange-400 outline-none"
        placeholder="Enter ticket title"
      />
      <p class="text-red-500 text-sm mt-1 hidden">Title is required</p>
    </div>

    <div>
      <label
        for="description"
        class="block text-sm font-medium text-gray-700 mb-1"
      >
        Description
      </label>
      <textarea
        id="description"
        name="description"
        rows="3"
        class="w-full border border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-orange-400 outline-none"
        placeholder="Enter ticket details"
      ></textarea>
    </div>

    <div>
      <label for="status" class="block text-sm font-medium text-gray-700 mb-1">
        Status <span class="text-red-500">*</span>
      </label>
      <select
        id="status"
        name="status"
        class="w-full border border-gray-300 rounded-md p-2 focus:ring-2 focus:ring-orange-400 outline-none"
      >
        <option value="open">Open</option>
        <option value="in_progress">In Progress</option>
        <option value="closed">Closed</option>
      </select>
    </div>

    <div class="flex gap-4">
      <button
        type="submit"
        class="bg-orange-500 hover:bg-orange-600 text-white px-5 py-2 rounded-md font-medium transition-colors"
      >
        Create Ticket
      </button>
      
      <button
        type="button"
        class="bg-gray-200 hover:bg-gray-300 text-gray-800 px-5 py-2 rounded-md font-medium transition-colors hidden"
      >
        Cancel
      </button>
    </div>
  </form>
</section> #}
